# Dependencies
node_modules/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code: keep shared launch config, ignore machine-local settings
.claude/settings.local.json

# Local-only: full backups & large source masters (not deployed/versioned)
*.zip
Source files/
LIMRA-Global-Gateway/
_render/

# Raw client-supplied logo/map source files — already processed into /assets
colleges/*.png
colleges/*.jpg
colleges/*.jpeg
colleges/*.webp
colleges/*.svg

# Hero prototype — kept locally as reference, not deployed
hero-preview.html
