# LIMRA Overseas Education — Netlify config
# Eleventy builds the blog/news into static HTML; everything else is copied
# through unchanged. Publish the generated _site/ directory.

[build]
  publish = "_site"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
